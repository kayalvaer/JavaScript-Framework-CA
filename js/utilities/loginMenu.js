/* import { getUsername } from "../static/dataStore.js"

export default function loginMenu() {
    const { pathname } = document.location;

    const username = getUsername();

    let logLink = `<a href="login.html" class="${pathname === "/login.html" ? "active" : ""}">Log In</a>`;

    if (username) {
        loglink = `<span> Hi ${username}</span>`;
    }

    console.log(username); */
    //const container = document.querySelector(".nav-container");

     /* container.innerHtml = `<ul class="nav-menu">
                                        <a href = "/" class="${pathname === "/" ? "active" : ""}">
                                            Home
                                        </a>
                                        <a href = "login.html" class="${pathname === "/login.html" ? "active" : ""}">
                                            Login
                                        </a>
    
                                    </ul>`;  */

//}