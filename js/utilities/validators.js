/* import { minimumPasswordLength } from "../setup-api/settings.js";

export function isEmailCorrect(email) {
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}

export function isPasswordCorrect(string) {
    return withNumber(string) && verifyLength(string);
}

function withNumber(string) {
    return /\d/.test(string);
}

function verifyLength(string) {
    return string.trim().length >= minimumPasswordLength;
} */