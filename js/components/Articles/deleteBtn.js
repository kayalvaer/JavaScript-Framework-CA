/* import { baseUrl } from "../../setup-api/api.js";
import { getTokenKey } from "../../static/dataStore.js";

export default function delItemBtn(id) {

    const container = document.querySelector(".deleteIcon");

//  container.innerHTML = `<button type="button" class="delBtn">Delete</button>`;

button.onclick = async function () {
    console.log(id);
};

} */